// 任务处理器：Worker的核心。
// 当收到一个任务时，它负责完整的执行生命周期管理：
// 从数据库获取任务详情，调用core中的Wasm执行器来运行Agent，处理成功或失败的结果，
// 并将最终状态和产出更新回数据库。
// 它能够处理独立的Agent运行和流水线中的分步运行。
// 如果一个流水线步骤成功，它会自动触发下一个步骤的任务消息。
// 如果失败，它会更新整个流水线的状态为failed并记录错误日志。